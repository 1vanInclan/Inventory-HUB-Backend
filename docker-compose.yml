
services:
  api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    # Aquí es donde sucede la magia:
    env_file:
      - .env 
    environment:
      - NODE_ENV=development